@model IEnumerable<BookStoreWeb.Models.Domain.Author>
@{
}

<div class="container pt-5">
    <div class="row">
        <div class="col-md-6">
            <a class="btn btn-dark" href="/Author/Add">Add more</a>
        </div>
    </div>

    <div class="row">
        <div class="col-md-6">
            <h2>Authors</h2>

            <table class="table table-striped table-bordered table-dark">
                <thead>
                    <tr>
                        <th>AuthorName</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.AuthorName</td>
                            <td>
                                <a class="btn btn-success" href="/Author/Update?id=@item.Id"><i class="bi bi-pen"></i></a>
                                <a class="btn btn-danger" onClick="return window.confirm('Are you sure?')" href="/Author/Delete?id=@item.Id"><i class="bi bi-trash"></i></a>
                            </td>
                        </tr>
                    }

                </tbody>
            </table>
        </div>
    </div>

</div>